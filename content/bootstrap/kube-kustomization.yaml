apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kube
  namespace: flux
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  interval: 30m
  sourceRef:
    kind: GitRepository
    name: kube
  path: ./content
  prune: true
  timeout: 5m
