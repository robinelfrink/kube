apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: nextcloud
spec:
  interval: 1h
  path: /kustomize/apps/nextcloud
  prune: true
  sourceRef:
    kind: GitRepository
    name: kube
  validation: client
  postBuild:
    substitute:
      dummy_var: dummy
    substituteFrom:
      - kind: Secret
        name: config
