{%- if config.apps | default([]) | length > 0 %}
deployments:
{%- for app in config.apps | default([]) %}
  - path: {{ app.app }}
    recursive: true
    vars:
      - values:
          app: {{ app }}
{%- endfor %}
{%- endif %}
