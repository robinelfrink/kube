apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: website-one
  name: website-one
  namespace: nextcloud
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: ${website_one_port}
  selector:
    app.kubernetes.io/component: app
    app.kubernetes.io/instance: nextcloud
    app.kubernetes.io/name: nextcloud
